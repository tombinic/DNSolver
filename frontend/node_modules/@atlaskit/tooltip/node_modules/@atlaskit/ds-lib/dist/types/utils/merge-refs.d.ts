import { Ref, RefObject } from 'react';
declare type Refs = Ref<HTMLElement | null> | RefObject<HTMLElement | null> | ((node: HTMLElement | null) => void);
/**
 * Assigns the node to all the refs passed in the argument.
 *
 * @param refs: An array of refs (as function or ref object)
 */
export default function mergeRefs(refs: Refs[]): (value: HTMLElement | null) => void;
export {};
